#cargo-features = ["build-std"]

#[package]
#name = "forcefield"
#version = "0.1.0"
#edition = "2024"

[workspace]
resolver = "3"
members = ["common", "citadel", "generator"]

[profile.release]
opt-level = 2
lto = "thin"
strip = true
